
# 闭环步进驱动板（ESP32-C6，42步进电机背装方案）文档整理
> 定位：推拉门/智能窗帘闭环步进驱动，**超低待机功耗**，支持手拉检测唤醒、CAN组网，板载磁编码器，电机尾部背贴PCB方案，主驱动DRV8874×2。
>
> **版本 v0.5（2026-09-14）** | 本次变更：**BOM 补全位号、封装、数量、装配面、DNP**，拆为"主器件 / 阻容无源 / 待填项"三张表；确认 KTH5701 封装为 QFN3×3-16L 或 DFN2×2.5-8L（立创仅在售 QFN，影响能否上机贴片）；CAN 接口 BOM 行同步改为 3P。
> 历史版本：v0.4（母线分压门控化、待机 0.25mW、更正τ算法）→ v0.3（移除CAN唤醒、补齐 Buck/Type-C/VM保护/控制架构）→ v0.2（32k晶振、重排GPIO、移除蜂鸣器与Qwiic）→ v0.1；备份见 `doc.md.bak`

## 设计规格速览（v0.3 新增）
|项目|指标|来源|
| ---- | ---- | ---- |
|输入电压|DC 12~24V 标称；**上限 36V**（Buck VIN 限制），推荐长期 ≤30V|二、七.1|
|步进输出|两相双极，单相电流由 VREF + R_IPROPI 设定（42步进典型 1.5A/相）|五.4|
|电机接口|4P（A+ A- B+ B-），两相四线|七.2|
|位置反馈|KTH5701 磁编码器，I2C 绝对角度，板载背装|五|
|通信|CAN 3P（SN65HVD231，默认睡眠）；Type-C 原生USB（烧录/调试）|七.3、七.4|
|待机功耗|≈**0.25mW @24V**（母线分压门控后≈0；若不门控则为 0.78mW）|六|
|深睡唤醒源|编码器 INT ／ RTC定时 ／ 干接点（与INT共线）／ WiFi；**CAN 不参与唤醒**|六|
|唤醒延迟|编码器 5Hz 采样 ≤200ms；25Hz ≤40ms|六|
|RTC 精度|±20ppm（≈±1.7s/天），依赖 32.768kHz 晶振|五.5|
|控制方式|闭环步进/微步进；硬件预留 FOC 三环（4路PWM + 两路低边电流采样 + 绝对角度）|十|
|机械|42电机尾部背贴，四角M3；反面器件高度分区 ≤3mm|三|

## 一、BOM元件清单
> v0.5：补全位号、封装、数量、装配面与 DNP 标注。"面"依据第三节的正反面分区规则给出**建议值**，最终以 Layout 为准；无源器件的位号由原理图分配，此处按功能分组给数量。

### 1.1 主器件与有源器件
|位号|模块|型号|关键参数|封装|数量|面|单价(JLC/TB)|备注|
| ---- | ---- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
|U1|主控|ESP32-C6-MINI-1|模组，含40MHz主晶振，**不含32k晶振**|SMD模组|1|正|22.5/14|天线端须悬出板边|
|U2|DCDC|TPP363070Q|Buck，Vin 3~36V，3A，400kHz，Iq 6µA|QFN2×3-12|1|正|3.9/2.5|输出设定3.35~3.4V|
|U3,U4|H桥|DRV8874PWPR|6A峰值，集成低边电流采样，睡眠<1µA|HTSSOP-16|2|正|7.2/2.8|正面铺铜+过孔散热|
|U5|编码器|KTH5701AQ3QNS|3D霍尔，16bit，I2C，待机1.4µA|QFN3×3-16L|1|**反**|—/6.8|中心磁铁区唯一器件；封装选型见1.3|
|U6|CAN|SN65HVD231DR|3.3V收发器，睡眠40nA|SOIC-8|1|反|10/1|置CAN连接器正下方|
|U7|USB|USBLC6-2SC6|D±/VBUS ESD阵列|SOT-23-6|1|正|—|靠Type-C连接器|
|Q1|指示|AO3401A|P-MOS −30V|SOT-23|1|正|—|WS2812供电开关；栅极100k上拉兼作GPIO8 strapping上拉|
|Q2|采样|P-MOS **≥40V**|母线分压高边门控|SOT-23|1|正|—|**待选型**，AO3401A的−30V不够|
|Q3|采样|NPN（MMBT3904等）|Q2栅极电平转换|SOT-23|1|正|—|—|
|D1|电源|SS14|40V/1A肖特基|SMA/DO-214AC|1|正|—|USB VBUS并入Buck输入|
|D2|电源|TVS（SMBJ26A~33A）|VM过压吸收|SMB|1|正|—|**待定**，先实测VM峰值（见五.8）|
|D3|指示|电源LED|低有效，接GPIO9|0603|1|正|—|与BOOT共用GPIO9|
|D4,D5|指示|WS2812B-V5 或 SK6812|**必须支持3.3V供电**|5050|2|正|—|普通WS2812B（VDD下限3.5V）不可用|
|D6|指示|同上|预留第3颗|5050|1|正|—|**DNP**|
|Y1|时钟|32.768kHz晶振|CL=12.5pF，±20ppm，**ESR≤70kΩ**|3215|1|正|—|贴模组GPIO0/1，下方禁走线|
|L1|DCDC|功率电感|**饱和电流≥3A**，电感值按手册|一体成型（**待定**）|1|正|—|按手册推荐表定稿|
|L2,L3|滤波|MGLB2012M601T1R2|600Ω@100MHz，DCR 0.25Ω，1.2A|0805|2|正|—|ESP32／编码器各一|
|J1|接口|DC输入座|12~24V，**防呆**|按料号（C2847863/C52190570）|1|正|—|无防反接保护|
|J2|接口|Type-C母座|烧录调试|SMD（C2988369）|1|正|—|配CC 5.1k×2、ESD阵列|
|J3|接口|电机端子|A+ A− B+ B−|4P|1|正|—|禁止带电插拔|
|J4|接口|CAN端子|CANH/CANL/GND|**3P**（v0.3起由2P改）|1|正|—|补GND解决跨电源共模|
|SW1,SW2|按键|侧按|BOOT=GPIO9↔GND；RESET=EN↔GND|侧按SMD|2|正|—|板边，侧面按压|
|—|结构|磁铁|轴心12mm包络内，气隙1~3mm|—|1|—|—|另配|

### 1.2 阻容与无源器件（按功能分组）
|模块|参数|封装|数量|面|备注|
| ---- | ---- | ---- | ---- | ---- | ---- |
|DC入口|100µF/35V 电解，ESR 260mΩ（JARSON RX1V101M0606）|—|1|正|**建议改50V**（见九.3）|
|Buck输入|10µF/50V|0805|1|正|贴VIN脚|
|Buck自举|0.1µF|0402|1|正|BST~SW|
|Buck输出|按手册（值待定）|—|2|正|另预留100µF固态位×1（DNP）|
|Buck反馈|1%分压（值待定）|0402|2|正|设定3.35~3.4V|
|晶振负载|15~18pF C0G ±5%|0402|2|正|按晶振CL计算|
|晶振偏置|5~10MΩ|0402|1|正|**DNP**，预留焊盘|
|ESP32支路|22µF（磁珠后）|0805|1|正|—|
|编码器支路|10µF（磁珠后）|0805|1|反|—|
|CAN支路|10µF|0805|1|正|**不加磁珠**|
|各IC去耦|0.1µF|0402|6|正/反|贴电源脚1~2mm内|
|磁珠直通位|0Ω|0805|2|正|**DNP**，实测噪声达标可直接省磁珠|
|母线分压|100k + 8.2k，1%|0402|2|正|比值0.0758|
|分压滤波|10nF|0402|1|正|τ=76µs|
|门控外围|栅极上拉 + 基极电阻|0402|2|正|配Q2/Q3|
|EN复位|10k + 1µF|0402|2|正|乐鑫推荐RC|
|GPIO9上拉|10k|0402|1|正|满足SPI boot=1|
|WS2812外围|栅极上拉100k + DIN串联100Ω|0402|2|正|上拉兼GPIO8 strapping|
|电源LED限流|按LED选（约1k）|0402|1|正|—|
|nSLEEP下拉|10k|0402|1|正|上电默认电机断电|
|nFAULT上拉|10k|0402|1|正|两片线与共用|
|IMODE|20k（每片一颗）|0402|2|正|cycle-by-cycle + auto-retry|
|R_IPROPI|3.5k 1%（每片一颗）|0402|2|正|按 ITRIP=1.5A 核算，见五.4|
|IPROPI滤波|10nF（每片一颗）|0402|2|正|过大将拖慢斩波响应|
|VREF分压|10k + 22k，1%（两片共用）|0402|2|正|V_VREF≈2.34V|
|VM就近|100µF/35V固态 + 10µF + 0.1µF（每片一组）|—|2+2+2|正|贴VM引脚|
|编码器I2C上拉|4.7k（值待定）|0402|2|**反**|中心区，随编码器就近|
|CAN终端|120Ω + 焊桥/跳线|0805|1|反|菊花链仅两端保留|
|CAN Rs上拉|10k|0402|1|反|10k上拉→上电默认睡眠|
|Type-C CC|5.1k|0402|2|正|UFP识别，漏掉则C口线不供电|
|输入保护|TVS + 自恢复保险丝|—|各1|正|**待定**，见九.2|

### 1.3 待填项（确认后定稿）
|#|事项|说明|
| ---- | ---- | ---- |
|1|**KTH5701 封装选型**|立创在售的是 **QFN3×3-16L**（KTH5701AQ3QNS，C25838714，约¥7.25，可上机贴片）；**DFN2×2.5-8L**（AQ3DNE）更小但只在代理渠道有货，需手焊。**要走 JLC 上机贴片就选 QFN**|
|2|KTH5701 温度等级|消费级 AQ3（−40~+85℃）vs 工业级 AQ2（−40~+105℃）。板子背贴电机、环境温度偏高，建议评估工业级|
|3|Buck 电感值 / 输出电容 / 反馈分压阻值|按 TPP363070Q 数据手册推荐表定稿|
|4|DC 连接器封装|按最终选定的立创料号（C2847863 / C52190570）|
|5|分压门控 P-MOS 型号|需 ≥40V 耐压 SOT-23|
|6|TVS、自恢复保险丝型号|先实测手拉 VM 峰值再定（见五.8）|
|7|电解电容耐压 35V→50V|待拍板（见九.3）|
|8|阻容封装统一|0402 还是 0603；反面限高 ≤3mm，两者都满足|

### 备选元件（评估替换，主方案未使用）
1. 供电（早期两级方案，已废弃）：WL9005P3-33（LDO）
2. 步进驱动备选：TMC2209、TMC2240、TMC2225、TMC2226
3. 编码器备选：MT6701、AS5600、AS5047P
4. CAN收发器：**不做备选替换**。本板 v0.3 起不启用 CAN 唤醒（原因见五.3），SN65HVD231 仅承担通信；曾评估的 SN65HVD230（standby 可监听总线）已放弃。

> TMC2209备注：待机12V功耗＜2mW(160µA)，下文附带TMC2209完整待机控制时序（仅备选评估使用，本板主方案DRV8874）

## 二、供电架构：单级DCDC直供3.3V
输入：**DC 12~24V**，单级Buck输出3.35~3.4V（不使用LDO）
> 输入上限由 Buck 决定：**TPP363070 的 VIN 上限为 36V**，实际系统最高输入按 36V 计，推荐长期工作 ≤30V。DRV8874 的 VM 极限 40V 不构成瓶颈，**任何场合都不应以 40V 作为输入上限使用**。
> 设计理由：全器件电压工作区间3.0~3.6V，buck纹波+瞬态跌落叠加后最低≥3.15V；省去LDO，减少发热与BOM数量。
> 抬高输出至3.35~3.4V：补偿磁珠DCR压降（ESP32 TX峰值382mA ×0.1Ω≈40mV），模组端稳压落在标称3.3V附近，上限3.47V <3.6V最大额定。

### 电源树
```
DC输入12~24V → 入口100µF/35V液态电解（阻尼）
├─ TPP363070 → 3.3V骨干轨（3.35~3.4V）
│  ├─磁珠 +22µF +0.1µF → ESP32-C6-MINI
│  ├─磁珠 +10µF +0.1µF → KTH5701编码器
│  ├─10µF +0.1µF → SN65HVD231 CAN
│  └─LED / WS2812（P-MOS开关，上电默认断电）
├─[高边P-MOS门控·GPIO7] + 100k+8.2k分压 + 10nF → GPIO2 ADC母线电压采集
└─DRV8874×2：VM功率轨就近固态100µF/35V +10µF+0.1µF贴VM引脚
```

### 支路滤波分工
|支路|措施|类型|作用|
| ---- | ---- | ---- | ---- |
|DC入口母线|100µF/35V液态电解，ESR≈260mΩ|阻尼|吸收线缆电感谐振、插拔浪涌；高ESR是设计目标|
|ESP32-C6-MINI|磁珠+22µF+0.1µF，π型滤波|射频降噪|板上唯一射频器件，抑制Buck纹波、振铃，保护WiFi接收灵敏度|
|KTH5701编码器|磁珠+10µF+0.1µF|隔离滤波|闭环传感器，隔离CAN、ESP发射脉冲、开关电源噪声|
|SN65HVD231|10µF+0.1µF本地解耦|电源解耦|CAN是噪声源，**不加磁珠**|
|DRV8874 VM功率轨|每颗就近固态100µF+10µF+0.1µF贴脚|功率解耦|安培级斩波电流由本地电容储能，不属于3.3V支路|

> Buck输入端：VIN引脚旁放置10µF陶瓷电容，和入口电解配合。

### 母线电容选型与ESR梯度设计
**ESR逐级递减**：入口液态260mΩ（阻尼）→固态≤60mΩ（储能）→贴脚陶瓷mΩ级（高频边沿电流）
1. DC入口：JARSON RX1V101M0606，100µF/35V液态，ESR260mΩ
2. DRV8874就近：CH SV101M350E5R8R4Z00ZZ固态100µF/35V，ESR30mΩ；备选Ymin VPXC0581V101MVTMLR
3. Buck输入：10µF陶瓷贴VIN脚

> ⚠️ 待定：35V 电解用于 24V 母线仅 1.46× 降额，且手拉回灌会抬高 VM，建议改 50V（见第九节）。

### Buck 外围设计（v0.3 补齐）
原文档只写了芯片与三颗滤波电容，以下为原理图阶段必须落实的其余外围：

|项|要求|说明|
| ---- | ---- | ---- |
|功率电感|按数据手册 3A/400kHz 档推荐值选取；**饱和电流 ≥ 1.5× 最大负载电流**（≥1.5A×1.5≈2.3A，建议 ≥3A）|开关节点走线短而粗，远离编码器与晶振区|
|输出电容|按手册推荐值并联，另预留一颗 100µF 固态位（应对电机启动瞬间的母线跌落）|模组侧另有 22µF+0.1µF（见电源树）|
|反馈分压|VO = VFB × (1 + R1/R2)，设定 **3.35~3.4V**；VFB 与推荐阻值以数据手册为准，分压电阻用 1%|分压电阻长期耗电，选大阻值档|
|自举电容|BST~SW 之间 0.1µF（按手册），耐压 ≥16V|靠近芯片引脚|
|EN 脚|不直接接 VIN，建议留电阻分压，可做输入欠压锁定（UVLO）联动|手册已内置 UVLO，此处为可选增强|
|输入电容|VIN 脚旁 10µF 陶瓷（已有）|与入口电解配合|

> ⚠️ 全部数值需按 TPP363070Q 数据手册的推荐表定稿后填入原理图；本表的作用是**防止漏画**。

### 磁珠选型（ESP32与KTH5701共用同料号，减少BOM）
**选型要求**
- 阻抗：600Ω@100MHz，400kHz衰减约55dB；最低≥120Ω可用
- DCR ≤0.25Ω（硬上限0.4Ω），382mA压降≤95mV，保证芯片最低电压余量
- 额定电流≥0.7A，推荐≥1A

选定：MGLB2012M601T1R2（麦捷，600Ω，DCR 250mΩ，1.2A）
升级备选：BLM21PG601SN1（村田，600Ω，0.1Ω，1.5A）
> PCB预留0Ω电阻位：实测噪声达标可直接直通，省去磁珠，不用改版。

### 电容布局硬性规则
1. 0.1µF必须贴芯片电源引脚1~2mm内；大电容放其后。小电容处理高频边沿电流。
2. 电容GND就近打via，和芯片GND构成最小电流环路。
3. 磁珠**两侧必须放置电容**（π结构），防止磁珠与芯片内部电容谐振放大噪声。

## 三、地平面、回流与PCB布局（4层板，板厚1.2mm）
叠层：
- L1 顶层：信号+元件
- L2：完整GND地平面，仅过孔穿透，不开槽
- L3：电源层，3.3V全域铺铜+VM电机功率分区；Buck开关节点走L1/L4局部，不进电源层
- L4 底层：信号+元件（编码器、被动器件）

> 4层核心理由：电机PWM电流回流依托L2地平面，自动避开编码器敏感区域；2层板很难在编码器下方铺连续干净地。
> 铜厚：JLC标准外层1oz，内层0.5oz；大电流走线全部放在外层。天线区域四层全部挖空铜皮（L1/L2/L3/L4）。

### 回流与地设计纪律
- 电机大电流回流**星形汇聚到电源入口**，不穿越3.3V信号地平面。
- DRV8874功率地独立回流；编码器与主控区域保持完整地平面。
- DRV OUT1/OUT2电机相线，从板边缘布线，绕开板中心编码器区；交变磁场是编码器主要干扰源。
- 天线净空：ESP32-C6-MINI天线端悬出板边，远离42电机金属壳体（金属会屏蔽WiFi）。
- **L4走线下方（L3层）在编码器与晶振区域应铺GND铜并打缝合过孔**，避免参考面为电源层造成回流不连续。

### 32.768kHz 晶振布局要求（硬性）
- 紧靠 ESP32-C6-MINI 的 GPIO0/GPIO1 放置，走线尽量短（建议 ≤5mm）、**两根等长对称**。
- 晶振与负载电容**下方及邻近区域禁止走线、禁止铺其他网络铜皮**，整块做包地处理并打过孔。
- 远离 MCPWM 输出、DRV8874 开关节点、电机相线、Buck 功率电感至少 5mm。
- 两侧负载电容的地要单独就近回到模组 GND，不可与功率地共用回流路径。

### 正反面器件分配（板背贴42步进电机尾部，机械限高约束）
✅ **反面（朝向电机），分双区**
1. 中心磁铁区：轴心12mm圆，磁铁旋转包络；气隙1~3mm
    - 允许：KTH5701编码器（0.8mm）+就近滤波阻容、I2C上拉
    - 禁止：任何其他器件
2. 外围区（中心以外，垫柱5~10mm）
    - 允许：0402~0805阻容、磁珠、SOT-23、SOD-123、CAN 120Ω等≤3mm器件
    - 禁止：连接器、发热器件、超高器件
- BOOT/RESET侧按按键：板边外围区，侧面按压
- SN65HVD231+解耦+120Ω：CAN连接器背面正下方，缩短回路

✅ **正面（朝外）**
- ESP32-C6-MINI（天线必须外悬，反面放不下）
- 32.768kHz晶振+负载电容（紧贴模组GPIO0/GPIO1侧，见上）
- TPP363070+功率电感+Buck输出电容
- DRV8874×2：正面铺铜+过孔散热，远离板中心，减少磁场干扰编码器
- 入口电解+DRV就近固态电容（高度高，仅正面）
- 全部对外连接器：DC、电机4P、CAN、Type-C
- LED、WS2812

> 硬性约束：磁铁旋转包络内禁布器件；指示灯不放反面；发热器件不放反面；按键只能侧面按压。

### 机械配合
四角M3安装孔对齐电机后盖；垫柱高度 = 后轴伸出长度 − 磁铁厚度 + 1~3mm
垫柱建议≥4mm起步，反面器件高度分区核算，器件与结构间隙≥0.5mm。

## 四、GPIO引脚分配表（ESP32-C6）
> v0.2 重排：GPIO0/1 让给 32.768kHz 晶振；CAN RXD 移出 RTC/LP 域；蜂鸣器、Qwiic 相关引脚全部释放。

|GPIO|功能|方向|域|备注|
| ---- | ---- | ---- | ---- | ---- |
|GPIO0|XTAL_32K_P|—|LP|32.768kHz晶振，**不可再作他用**|
|GPIO1|XTAL_32K_N|—|LP|32.768kHz晶振，**不可再作他用**|
|GPIO2|输入电压采集|ADC CH2|LP|100k+8.2k分压+10nF，经高边门控，母线电压检测|
|GPIO3|KTH5701 INT|输入|LP|LP ext1唤醒；手拉/手推检测|
|GPIO4|IPROPI-1|ADC CH4|LP|DRV8874#1电流反馈|
|GPIO5|IPROPI-2|ADC CH5|LP|DRV8874#2电流反馈|
|GPIO6|PMODE（两片DRV共用）|输出|LP|低=PH/EN、高=PWM、Hi-Z=独立半桥；上电默认低|
|GPIO7|母线分压门控|输出|LP|高边P-MOS栅极（经NPN电平转换），见五.1|
|GPIO8|WS2812供电开关|输出|HP|P-MOS栅极+100k上拉，**上电=高（灯灭）且满足strapping=1**|
|GPIO9|电源状态LED|输出|HP|低有效+10k上拉；满足SPI boot=1，BOOT按键直连GPIO9到GND，**严禁加消抖电容**|
|GPIO10|DRV8874#1 IN2（EN）|输出|HP|MCPWM|
|GPIO11|DRV8874#1 IN1（PH）|输出|HP|MCPWM；与GPIO10成对|
|GPIO12/13|USB D-/D+|—|—|Type-C烧录调试|
|GPIO14|DRV8874#2 IN2（EN）|输出|HP|MCPWM|
|GPIO15|DRV8874#2 IN1（PH）|输出|HP|MCPWM；与GPIO14成对（GPIO15为strapping脚，见下）|
|GPIO16|nSLEEP（两片DRV共用）|输出|HP|10k下拉，上电默认电机断电|
|GPIO17|nFAULT（两片线与）|输入|HP|10k上拉，故障检测|
|GPIO18|CAN TXD|输出|HP|UART1 TX|
|GPIO19|CAN Rs|输出|HP|10k上拉；上电CAN默认睡眠|
|GPIO20|CAN RXD|输入|HP|**已移出RTCIO**；CAN三线集中在GPIO18/19/20|
|GPIO21|WS2812 DIN|输出|HP|100Ω串联电阻，RMT驱动，状态指示灯|
|GPIO22|I2C SDA|双向|HP|KTH5701磁编码器（**仅此一路从设备**）|
|GPIO23|I2C SCL|双向|HP|KTH5701磁编码器|

占用24脚，**LP域（GPIO0~7）已用满，无空闲GPIO**。

### 三类特殊引脚（Strapping / ADC1 / RTCIO）

|类别|范围|关键约束|
| ---- | ---- | ---- |
|**Strapping**|GPIO4/5/8/9/15|复位释放瞬间锁存（tSU=0ms / tH≥3ms）；**GPIO8=0 且 GPIO9=0 为非法组合**；**不得加消抖电容**（消抖只能做在固件里）|
|**ADC1**|GPIO0~6，7路，无ADC2|全部落在RTCIO域；GPIO0/1被晶振占用，实际可用5路，本项目用满3路，**无剩余通道**|
|**RTCIO（LP域）**|GPIO0~7，8个|深睡期间仍供电：**唯一支持ext1唤醒**、可保持电平（hold）、可复用LP外设、LP核可直接访问|

### Strapping 脚上电电平核查表（ESP32-C6 共5个）
|脚|上电锁存电平|由谁决定|判定|
| ---- | ---- | ---- | ---- |
|GPIO4（MTMS）|0|IPROPI-1 的 R_IPROPI 对地|✅ 仅影响SDIO采样沿，本设计不用SDIO|
|GPIO5（MTDI）|0|IPROPI-2 的 R_IPROPI 对地|✅ 同上|
|GPIO8|1|WS2812 P-MOS栅极100k上拉（被动）|✅ SPI boot无关；下载模式要求GPIO8=1，满足。**严禁加下拉**|
|GPIO9|1|10k上拉 + LED|✅ SPI boot=1|
|GPIO15|低|DRV8874#2 IN1 内部下拉（需实测确认）|⚠ 仅在选择JTAG信号源时参与判定（需配合eFuse），默认不影响启动|

> 关键纪律：**GPIO8 只允许上拉或由100k上拉决定，绝不能加下拉电阻**——GPIO8=0 且 GPIO9=0 是非法boot组合，会导致无法进入下载模式。其 strapping 电平由被动上拉决定，不依赖任何芯片的上电行为。

### GPIO与外设设计要点
> 引脚类别（strapping / ADC1 / RTCIO）与各自的上电约束见本节前两张表，此处不再重复。

1. ADC 已无剩余通道（见上表），后续若要增加模拟采样，只能改走外部 I2C ADC。
2. 单I2C总线（GPIO22/23）：仅接KTH5701编码器。原LP I2C（GPIO6/7）随Qwiic接口一并取消。
3. 引脚成对排布：IPROPI-1/2相邻（GPIO4/5）；DRV控制成对（10/11、14/15）；CAN三线集中在18/19/20。
4. **JTAG 提示**：GPIO4~7 是 RISC-V JTAG 的默认复用脚（MTMS/MTDI/MTCK/MTDO），本设计分别用作 IPROPI-1、IPROPI-2、PMODE、分压门控 → **默认的外部 JTAG 调试不可用**，调试只能走 USB-Serial-JTAG（GPIO12/13）。若确需外部 JTAG，需在 ESP-IDF 中把 JTAG 信号重映射到其他 GPIO。
5. 外部干接点唤醒只能与编码器INT（GPIO3）**合并唤醒线**（LP域已无空脚），由固件从"角度变化 vs 电平变化"上区分。
6. EN复位电路：EN脚10k上拉+1µF到GND（乐鑫推荐RC上电复位），RESET按键拉EN到GND。

## 五、外设硬件电路细节
### 1. 母线电压采集（高边门控式，v0.4 重算）
VM母线经**高边P-MOS门控**（GPIO7控制）→ 100k+8.2k 1% 分压 → 10nF滤波 → GPIO2 ADC。

**标定数据**
- 分压比 = 8.2/(100+8.2) = **0.0758**（与原 1M+82k 完全相同）
- 24V→1.82V；30V→2.27V；36V→2.73V；40V→3.03V（仅作极限参考，已超 Buck 上限）
- 源阻抗 = 100k∥8.2k = **7.6kΩ**（原方案 76kΩ，ADC 驱动条件改善 10 倍）

**为什么门控后要"降阻+降容"（v0.4 结论）**
门控把"单次测量"与"平均功耗"解耦了，因此可以主动降低阻值换取速度与精度：

|量|公式|关键性质|
| ---- | ---- | ---- |
|建立时间常数|τ = **(R1∥R2)** × C|注意是**并联值**，不是 R1+R2|
|单次导通时间|t_on ≈ 5τ|建立到 0.3% 精度|
|单次测量能耗|E ≈ **0.35 × VM² × C**|**与分压阻值无关**|
|平均功耗|P_avg = E × 测量频率|只由滤波电容 C 与测量频率决定|

> **v0.3 错误更正**：v0.3 曾把 τ 误算为 (R1+R2)×C，得出 108ms / 540ms 建立时间。实际应为并联值——1M+82k 配 100nF 的 τ 只有 **7.6ms**，5τ ≈ 38ms。此更正不影响"门控可行"的结论，反而说明门控比原先估计的更好用。

**推荐设计点（100k + 8.2k + 10nF + 高边门控）**
|项|值|
| ---- | ---- |
|分压比|0.0758（24V→1.82V）|
|源阻抗|7.6kΩ|
|τ|7.6kΩ × 10nF = **76µs**|
|单次导通|0.5~1ms|
|单次测量能耗|0.35 × 24² × 10nF = **2.0µJ**|
|平均功耗 @1Hz|**2µW**|
|平均功耗 @10Hz|**20µW**|
|对比：不门控的 1M+82k|533µW（**降低 266 倍**）|

> **门控的收益边界**：令 0.35·VM²·C·f 等于不门控的 VM²/R_total，得临界频率 f = 1/(0.35·C·R_total)。
> 本设计（100k+8.2k，10nF）的临界频率 ≈ **2.6kHz**——即使每秒测几百次，门控仍然划算。
> 若保持 1M+82k + 100nF，临界频率只有 **26Hz**。**这是"降阻+降容"的第二个好处：把门控的适用范围拉宽约 100 倍。**

**低阻带来的附带好处**
1. ADC 源阻抗从 76kΩ 降到 7.6kΩ，引脚漏电流引入的静态误差等比缩小，固件标定更简单。
2. 门控 MOS 若失效短路，持续功耗 5.3mW（高阻版本为 0.53mW）——两者都可忽略，**失效模式安全**，测量功能不受影响。

> ⚠️ **仍只能门控高边，绝不能门控低边**：若开关串在 8.2k 下方，关断时上臂电阻会把 ADC 节点抬到 VM 电位（24V），超引脚耐压、读数失真并有损伤风险。
> ⚠️ **与过压保护互斥**：门控后固件采样存在盲区（长度取决于测量频率），**若要用母线电压做硬件级过压保护，则不能门控**——过压必须由 TVS 等硬件兜底（见五.8）。

> ⚠️ **两个选择是绑定的**：若最终决定**不门控**，必须把分压改回 1M+82k（100nF）；否则 100k+8.2k 会持续消耗 **5.3mW**，比原来还差 10 倍。
> ⚠️ **门控 P-MOS 选型**：接在 24V 母线上，**耐压须 ≥40V**，AO3401A 的 −30V 不够用；栅极需 NPN 或小 N-MOS 做电平转换。器件待定。

### 2. WS2812/SK6812状态灯（2~3颗，默认贴2颗，预留第3颗）
- 供电经独立P-MOS开关（GPIO8），栅极100k上拉：**上电默认断电**，且该上拉同时用作GPIO8的strapping上拉。
- DIN=GPIO21串联100Ω电阻；3.3V供电为规格下限，固件禁止全白高亮，避免过载。
- ⚠️ **选型必须明确**：普通WS2812B的VDD规格下限为3.5V，3.3V属超规使用；应选 **WS2812B-V5 / SK6812** 等支持3.3V的型号。
- ⚠️掉电顺序：先拉低DIN，再关闭供电，防止ESD寄生漏电造成幽灵微光。
- DIN建议加对地下拉，避免MCU复位期间引脚浮空被干扰。

**颜色状态定义**
- 绿常亮：运行唤醒
- 绿慢闪：就绪待命
- 灭：深睡
- 红闪N次：故障码：1过流 /2过温 /3编码器失效 /4CAN掉线（过流/过温由DRV8874 nFAULT上报）
- 蓝闪：CAN总线活动
- 黄：配网模式
- 白渐变：OTA升级
- 紫：手拉启动接管

### 3. CAN总线 SN65HVD231
Rs引脚GPIO19，10k上拉，上电默认进入睡眠（40nA）；固件拉低Rs进入正常工作模式（tWAKE≈3µs）。
- **CAN 不参与唤醒（v0.3 正式决定）**：SN65HVD231 睡眠时**驱动器与接收器全部关断**，总线引脚高阻、**RXD 恒为高电平**，退出睡眠只能由控制器拉低 Rs（tWAKE≈3µs）。所谓"总线显性位唤醒"是 SN65HVD230 的 standby 模式才具备的特性，本板不采用——v0.1 文档中"支持总线唤醒"的说法已作废。
- 配套：RXD 已移至 GPIO20（非LP域），与"不唤醒"的定位一致；收发器睡眠期间与 GPIO3 的编码器 ext1 唤醒线完全解耦，互不干扰。
- 若未来确需 CAN 唤醒，属于**重新选型**（更换收发器 + RXD 放回 LP 脚 + 重算待机电流），不在本版设计范围内。
- 板载120Ω终端电阻；菊花链拓扑，**只在总线两端节点保留120Ω，中间节点拆除**（建议用焊桥/跳线实现，便于现场切换）。
- 所有节点必须共地，CANH/CANL用双绞线。⚠️ 当前CAN接口为2P，不引出GND，节点共地依赖DC供电回路；若跨电源节点组网，应改用3P接口。

### 4. DRV8874 外围配置（v0.2新增，原文档缺失）
DRV8874 除 IN1/IN2/nSLEEP/nFAULT 外，还有 4 组必须处理的配置项，缺一不可：

|引脚|配置方式|取值/说明|
| ---- | ---- | ---- |
|PMODE|GPIO6，两片共用，MCU控制|低=PH/EN、高=PWM、Hi-Z=独立半桥。**不用电阻配置死**，由单片机按运行状态切换；上电默认低（PH/EN安全态）|
|IMODE|电阻strap|推荐 20kΩ 对地 = cycle-by-cycle + auto-retry + 斩波 + 过流保护（推拉门场景自动重试更合适）|
|VREF|两片共用一组3.3V电阻分压|设定斩波阈值 ITRIP，必须落在数据手册推荐范围内|
|R_IPROPI|每片一颗，对地|将IPROPI电流转成电压供ADC读取，1%精度|
|nFAULT|两片线与→GPIO17|⚠️ 线与后**无法区分是哪一片报故障**，只能判定"有故障"；若需区分需拆成两个GPIO|

**参数计算**
```
IPROPI(µA) = 负载电流(A) × A_IPROPI(µA/A)     A_IPROPI Typ = 450 µA/A
V_IPROPI(V) = IPROPI × R_IPROPI
ITRIP(A)    = V_VREF / (R_IPROPI × A_IPROPI)
```
示例（42步进典型 1.5A/相）：VREF 取 2.34V（3.4V 经 10k/22k 分压）→ R_IPROPI = 2.34/(1.5×450µA) ≈ **3.5kΩ 1%**，此时 1.5A 对应 V_IPROPI≈2.36V，在 ADC 量程内。
- IPROPI 对地并 10nF 滤波（过大将拖慢斩波响应）；DRV8874 内部对 IPROPI 有钳位，可保护ADC。
- ADC 读到的 IPROPI 既可用于**堵转/堵门检测**，也可作为FOC电流环的相电流反馈（低边采样）。
- ⚠️ 实际 ITRIP 与 R_IPROPI 需按最终选定的电机相电流额定值核算后定稿。

### 5. 32.768kHz RTC 晶振（v0.2新增）
连接到 GPIO0（XTAL_32K_P）/ GPIO1（XTAL_32K_N），作为 RTC_SLOW_CLK 时钟源。

**选型与配套**
|项目|要求|
| ---- | ---- |
|晶振|32.768kHz，±20ppm，CL=12.5pF，**ESR ≤ 70kΩ**（乐鑫硬性要求）|
|负载电容|C1=C2 ≈ 2×(CL − C_stray)，杂散取3~5pF → **15~18pF，C0G/NP0，±5%**|
|偏置电阻|5MΩ < R ≤ 10MΩ，**一般不上件**，预留焊盘（DNP）|

**为什么必须加**
深睡RTC定时精度从内部RC的 **±5%（≈±72分钟/天）** 提升到晶振的 **±20ppm（≈±1.7秒/天）**。这是"定时开关窗帘"和"手拉速度判定/位置推算"能够成立的前提，RC时基无法支撑。

**固件配置**
- `CONFIG_RTC_CLK_SRC_EXT_CRYS` / `esp_clk_slow_src_set(SOC_RTC_SLOW_CLK_SRC_XTAL32K)`
- 保留晶振不起振时回退 RC_SLOW 的看门狗逻辑，并上报故障码。
- 若不启用该时钟源，GPIO0/GPIO1 可恢复为普通GPIO——**本板已按启用晶振设计，不可再作他用**。

### 6. 睡眠与使能控制（v0.3 扩充）
**全部睡眠/使能控制脚一览**

|控制对象|引脚|睡眠/关闭电平|决定上电默认的外围|深睡时状态|
| ---- | ---- | ---- | ---- | ---- |
|DRV8874 ×2（nSLEEP）|GPIO16|低=睡眠（低有效）|10k 下拉|H桥断电|
|DRV8874 ×2（PMODE）|GPIO6|上电低=PH/EN安全态|内部下拉|安全态|
|CAN 收发器（Rs）|GPIO19|高=睡眠|10k 上拉|睡眠 40nA|
|WS2812 供电（P-MOS）|GPIO8|高=断电|100k 上拉（兼 strapping）|断电|
|KTH5701 编码器|—|I2C 配置|—|1.4µA 基线／25µA 采样|
|Buck TPP363070|—|无 EN 控制|—|常工作（供 ESP32）|

> 上电瞬间全部外设处于安全状态，**不依赖 GPIO HOLD**；深睡时 GPIO 浮空，依靠外部上下拉维持安全电平。

**板载部件睡眠控制能否合并成一根线？（v0.3 分析结论：不合并）**

|候选合并|可行性|结论|
| ---- | ---- | ---- |
|两片 DRV8874 的 nSLEEP|已合并，两片永远同态|✅ 保留|
|nSLEEP ＋（Rs／WS2812）|❌ **极性相反**：nSLEEP 低=睡眠，而 Rs、WS2812 都是高=睡眠/断电。直接并线会得到"电机已断电、收发器却已唤醒"或反之的错误组合；加反相级（1颗2N7002+电阻）只为省 1 个 GPIO，不划算|不合并|
|CAN Rs ＋ WS2812 供电|⚠️ **极性相同，但会锁死状态组合**：两者都是"低=工作/供电"。合并后无法表达「手拉接管时灯要亮、CAN 保持睡眠」——而这恰是低功耗手拉唤醒后最常用的状态（唤醒收发器要付出 mA 级电流，是 µA 级待机的几十倍）|不合并|

> **结论**：硬件上不合并。真正的"统一控制"应在**固件层**用一个 power_state 状态机编排（见十.3），每个状态分别定义 nSLEEP／Rs／WS2812／编码器采样的组合；硬件并线会永久丧失状态组合能力。
> 注：v0.4 起 GPIO7 已分配给母线分压门控，LP域用满——但**这更加不能成为并线的理由**，因为并线损失的正是状态组合能力，而省下的引脚数（1个）远抵不上它的代价。
> 第二条理由：**WS2812 断电有先后顺序要求**（先拉低 DIN 再断电，见五.2），必须由固件单独控制关断时机，不能与别的控制同步粗暴切换。

### 7. Type-C 接口电路（v0.3 补齐）
|项|做法|原因|
| ---- | ---- | ---- |
|CC1 / CC2|各串 **5.1kΩ 下拉到 GND**|标识为 UFP 受电设备；漏掉则 C 口线不供电、无法识别|
|VBUS|SS14 肖特基并入 Buck 输入（见七.4）|与 DC 输入二极管反向隔离|
|D+ / D−|加 ESD 阵列（如 USBLC6-2SC6），靠近连接器|热插拔 ESD|
|D+ / D− 走线|90Ω 差分阻抗、等长、尽量短、少打过孔|原生 USB 信号完整性|
|VBUS 检测|由母线分压 ADC 读取（USB 单独供电时约 4.6V）|固件据此判断"无 DC 输入"|
> ⚠️ 固件：USB 单独供电时 VM≈4.6V，仅比 DRV8874 最低工作电压 4.5V 高 0.1V，**必须禁止使能电机**。

### 8. VM 母线保护（v0.3 补齐）
**风险机理**：传动必须可逆（消防逃生要求），深睡时 H 桥处于高阻（coast）；手动推拉时电机作发电机，两相绕组经体二极管整流回灌 VM，即 VM ≈ 24V + 反电动势。

**量级估算**（42步进反电动势常数 Ke ≈ 5~10 V/kRPM 线电压）
|手拉转速|反电动势|VM 峰值|判定|
| ---- | ---- | ---- | ---- |
|100 RPM|0.5~1V|≈25V|✅ 安全|
|500 RPM|2.5~5V|≈27~29V|✅ 尚可|
|1200 RPM|6~12V|≈30~36V|⚠️ 逼近 Buck 上限|

> 结论：**常规手拉速度下风险不大**，只有极快的手拉（或低减速比直驱结构）才会逼近 36V。是否加保护器件应**先实测手拉最大速度下的 VM 峰值**再定，不要凭猜测上大规格器件。（v0.2 阶段"手拉会直接打穿 Buck"的说法偏激，此处更正为"需实测确认"。）

**保护手段**
1. **TVS（SMBJ 系列）**：泄放能量、限制尖峰。
   ⚠️ 固有局限：TVS 钳位电压 Vc 必然显著高于其关断电压，24V 母线可用的器件 Vc 普遍在 **40~48V**，**高于 Buck 的 36V 上限**——所以 TVS 只能"减少能量、缩短超压持续时间"，**不能保证 Buck 绝对不超压**。规格应在实测后于 SMBJ26A~33A 之间定稿。
2. **母线电容吸收**：入口 100µF 电解 + 就近固态 100µF（已有）。
3. **固件快速刹车（最有效）**：编码器 INT 唤醒后，**第一步不是平滑启动，而是把 H 桥切到 brake（两下管导通、绕组短接）**，把反灌能量消耗在绕组电阻上，确认 VM 回落后再进入助动控制。要求唤醒路径尽可能短（LP 核可先于主核执行最小刹车序列）。
4. **输入侧自恢复保险丝**（按峰值电流选取），与防反接的三重兜底并列。

## 六、低功耗唤醒方案（推拉门/窗帘应用）
> 场景：绝大多数时间处于深睡，**手推门/手拉窗帘 → 编码器检测转动，触发EXT1唤醒，电机平滑助动接管**
> 机械硬性前提：传动机构必须可逆（可手推，back-drivable）；蜗轮蜗杆自锁结构不可用，断电必须可手动推动（消防逃生要求）

### 唤醒源汇总
|唤醒源|机制|引脚|待机功耗|
| ---- | ---- | ---- | ---- |
|编码器KTH5701|角度变化产生INT中断，ext1唤醒|GPIO3（LP）|5Hz采样≈25µA；25Hz≈113µA；检测延迟200ms/40ms|
|RTC定时|LP定时器 + XTAL32K（±20ppm）|无引脚|晶振振荡器增量待实测（见第九节）|
|外部干接点/按键|ext1 GPIO唤醒，需与编码器INT**合并唤醒线**|GPIO3（LP，共线）|0|
|WiFi/ESP-NOW|轻睡模式数据包唤醒|不占引脚|轻睡电流|

> **CAN 不参与唤醒（v0.3 决定）**：唤醒源只保留"编码器 + RTC + 干接点 + WiFi"四项。深层原因见五.3——SN65HVD231 睡眠时接收器关断、RXD 恒高，物理上无法产生唤醒边沿；本版也不再为此更换收发器。

**整机待机总功耗 ≈ 0.25mW @24V（v0.4，母线分压改门控后）**
|项|电流/功耗|折算功耗|
| ---- | ---- | ---- |
|ESP32深睡|~10µA @3.3V|0.03mW|
|KTH5701|25µA @3.3V|0.08mW|
|TPP363070 输入侧|6µA @24V|0.14mW|
|**母线分压（门控 @1Hz）**|**2µW**|**≈0**|
|CAN收发器睡眠|40nA|≈0|
> 对比 v0.3（不门控）：**0.78mW → 0.25mW**，母线分压从最大单项（68%）变为可忽略。

### 固件核心要点
1. INT唤醒后：**第一步切 brake 泄放反灌能量**（见五.8），再读取编码器角度、防抖判定方向与速度，最后平滑启动电机助动。
2. 多圈位置跟踪、定期NVS保存位置；上电自学习限位。
3. 进入深睡前：Rs拉高、KTH5701配置为唤醒采样，配置ext1外部唤醒。
4. RTC定时任务（开关帘、定时同步）依赖XTAL32K；晶振异常时降级并上报。
5. **VM电压低于阈值时禁止使能电机**（USB单独供电时 VM≈4.6V，接近DRV8874最低工作电压4.5V）。

## 七、外部接口与接线规范
1. **DC供电（2P）**
    - 12V/24V推荐，**上限36V（Buck VIN限制）**。**无防反接保护，反接直接烧毁器件**；三重兜底：防呆连接器、丝印警示、上电前万用表确认极性。
    - 电源额定电流 ≥电机相电流 ×2相 ×1.5裕量。
2. **步进电机4P（A+ A- B+ B-）**：两相四线双极42步进电机。
    - 辨相：万用表测电阻，导通为同一绕组；线色不可靠。转向错误交换任意一相+/-。
    - 六线电机：中间抽头悬空；八线电机串联/并联接法，固件电流参数对应修改。
    - ❌禁止带电插拔电机线，断电等待电容放电完毕。
3. **CAN 3P（CANH/CANL/GND）**：菊花链，双绞线，节点共地，两端120Ω终端电阻。
    - **v0.3 起由 2P 改为 3P**：原 2P 不引出 GND，节点共地只能依赖 DC 供电回路；跨电源组网时共模电压无法保证落在 HVD23x 的 −2V~7V 范围内。补一根地线成本极低，直接规避。
4. **Type-C**：ESP32原生USB，烧录、串口调试，**不能驱动电机**。USB5V通过SS14肖特基并入Buck输入，DC供电时二极管反向隔离，互不冲突。接口电路（CC 5.1k×2、ESD、90Ω差分）见五.7。

## 八、首次上电检查清单
1. 未上电，万用表确认DC输入极性。
2. 电机四线绕组分辨正确，端子紧固。
3. 编码器磁铁安装到位，气隙1~3mm，同心无明显偏心。
4. **32.768kHz晶振已贴装，负载电容正确；上电后确认RTC时钟源为XTAL32K而非RC回退。**
5. **确认GPIO8上电为高电平**（用示波器看复位释放窗口），否则无法进入下载模式。
6. CAN总线所有节点共地，终端电阻仅总线两端。
7. 使用限流电源（0.5A）初次上电，观察电流、有无异常发热。
8. 固件先用小电流点动、低速试运行，确认转向与编码器读数方向一致，再提升工作电流。

## 九、待定决策与待核实项
### 待定决策（需拍板）
|#|事项|选项|状态|
| ---- | ---- | ---- | ---- |
|1|母线分压方案|**已推荐：高边门控 + 100k/8.2k + 10nF**（平均 2µW @1Hz，整机待机降到 0.25mW）。代价：占用 GPIO7，LP域用满无空闲脚；与"母线过压保护"互斥。回退方案：不门控 + 1M/82k + 100nF（0.53mW），保留 GPIO7 空闲——**两者绑定，不可只改其一**（见五.1）|待拍板|
|2|VM 保护器件|先实测手拉最大速度下的 VM 峰值，再于 SMBJ26A~33A 之间定稿；是否加自恢复保险丝（见五.8）|待定|
|3|母线电解耐压|35V → 50V（24V 系统降额更安全）|待定|
|4|~~CAN 唤醒取舍~~|已决定：**移除 CAN 唤醒**，CAN 仅作通信|✅ 已关闭|
|5|~~CAN 接口 2P→3P~~|已决定：**改 3P 补 GND**|✅ 已关闭|

### 待核实清单（对数据手册/实测逐条打勾）
|#|事项|为什么关键|
| ---- | ---- | ---- |
|1|KTH5701 是否真能在µA级低功耗下输出"角度变化中断"，阈值与延迟实测|整个手拉唤醒方案的地基，单点依赖|
|2|24V输入下TPP363070的空载输入电流实测（文档6µA可能为12V标称）|决定整机待机指标真假|
|3|32.768kHz晶振的深睡电流增量实测|datasheet未单列，直接影响待机口径|
|4|GPIO15、GPIO8 上电实际电平实测|strapping鲁棒性|
|5|手拉最大速度下的VM峰值（示波器实测）|决定TVS规格|
|6|DRV8874在1.5A/相下的壳温（正面铺铜+过孔是否足够）|热设计|
|7|ESP32-C6-MINI-1 是否已内置32k晶振；GPIO10/11 是否引出|下单前必须确认|
|8|磁珠DCR 250mΩ下模组端实际电压（3.4−0.095=3.305V，余量偏小）|供电余量|
|9|BOM 补位号/封装/数量/DNP 标注；各睡眠控制脚的外围电阻阻值定稿|原理图与贴片清单完整性|
|10|门控关断后实测 ADC 节点电压（应被 8.2k 拉到 0V），并确认门控 P-MOS 耐压选型|防止关断时节点被抬压、器件超压|

## 十、控制与固件架构（v0.3 新增）
> 项目代号 FocStepper。本板的硬件组合（4路PWM + 两路低边电流采样 + 绝对角度编码器）既能做闭环微步进，也能直接支撑 FOC 三环。本节先固定框架与分工，具体参数待样机标定。

### 10.1 信号链分工
|控制环|硬件资源|说明|
| ---- | ---- | ---- |
|位置环|KTH5701 绝对角度（GPIO22/23）|绝对角度，上电即知位置，无需找零|
|速度环|角度差分|由位置环输出|
|电流环|DRV8874 IPROPI（GPIO4/5 → ADC）|低边电流采样，两相各一路|
|PWM|MCPWM ×4（GPIO10/11/14/15），PMODE=高|四路独立占空比，可组合任意衰减模式|
> 该结构即参考项目（eetree #4598）用 SimpleFOC `StepperDriver4PWM` + `LowsideCurrentSense` 实现 FOC 的方式，本板按同一架构预留。

### 10.2 运行模式（按场景分档）
|模式|触发|实现要点|关注指标|
| ---- | ---- | ---- | ---- |
|手拉接管|编码器 INT 唤醒|**先 brake 泄放 → 判定方向与速度 → 电流环平滑助动**|唤醒延迟、无异响、无顿挫|
|闭环位置|CAN / WiFi 指令|位置环 + 速度环 + 电流环|定位精度、不丢步|
|保持|到位后|电流环给保持电流（可配置降至 30~50%）|温升|
|空闲|无指令|nSLEEP 拉低，H桥高阻|回到 µA 级待机|

### 10.3 状态机（power_state）
|状态|nSLEEP|CAN Rs|WS2812|编码器采样|灯色|
| ---- | ---- | ---- | ---- | ---- | ---- |
|深睡|低|高|断电|5Hz 唤醒采样|灭|
|唤醒接管|**高**|高（保持睡眠）|供电|25Hz|紫|
|运行|高|**低**|供电|25Hz+|绿|
|故障|低|按需|供电|保持|红闪码|

> 本表即五.6"睡眠控制不合并"的落地依据：同一个状态下三个控制脚取值不同，硬件并线无法表达。

### 10.4 必须落实的安全逻辑
1. **VM < 6V 禁止使能电机**（USB 单独供电时 VM≈4.6V，逼近 DRV8874 下限 4.5V）。
2. 上电自学习限位；多圈位置定期存 NVS。
3. nFAULT 触发 → 立即拉低 nSLEEP，记录故障码并闪灯告警。
4. CAN 丢帧 / 总线关闭 → 上报故障码 4，不影响本地控制。
5. 晶振不起振 → 降级 RC_SLOW 并上报。

# 附录：备选TMC2209 待机控制完整时序（仅评估方案，本板不采用）

# 附录：备选TMC2209 待机控制完整时序（仅评估方案，本板不采用）
> 引脚：STDBY、ENN、VREF；分5阶段，**退出待机寄存器会全部丢失，必须重配UART参数**
## 阶段1：初次上电 Power-On
- STDBY：拉低0；悬空内部下拉默认低，直接进入正常模式
- ENN：拉低0；建议硬件10k下拉，防止GPIO高阻误关功率级
- VREF：设定有效值；UART模式可悬空，不能超过2.5V
> 上电寄存器默认IHOLD=0，需要尽快UART配置保持电流。

## 阶段2：运行期间 Normal Operation
- STDBY持续低，ENN持续低，VREF有效电压；正常微步进运行。

## 阶段3：进入待机 Enter Standby（顺序不可颠倒）
1. 停止STEP脉冲，电机静止
2. 拉低ENN
3. 拉低VREF到0V
4. 拉高STDBY（最后一步）
> 拉高STDBY，芯片内部稳压断电，进入深度休眠，寄存器丢失。

## 阶段4：待机期间 During Standby
- STDBY=1，ENN=0，VREF=0；VCC_IO保持供电；静态功耗约2mW@12V。

## 阶段5：退出待机 Exit Standby
1. 第一步：拉低STDBY，内部电源上电，寄存器恢复出厂默认
2. 重写UART寄存器 IHOLD/IRUN/IHOLDDELAY等（**必做**）
3. 恢复VREF有效电压
4. ENN按需拉高/保持低电平

### 速查表
|阶段|STDBY|ENN|VREF|关键动作|
| ---- | ---- | ---- | ---- | ---- |
|上电|0|0|有效|下拉电阻防止误触发|
|运行|0|0|有效|正常驱动|
|进入待机|1|0|0|停电机→ENN→VREF→STDBY拉高|
|待机保持|1|0|0|VCC_IO供电，电机断电|
|退出待机|0|0|有效|拉低STDBY，**重配寄存器**|

> 核心陷阱：退出STDBY休眠，寄存器全部重置；不重写参数电机无保持力矩，无法锁轴。

## 参考项目链接
```
[https://github.com/makermoekoe/Picoclick-C3](https://github.com/makermoekoe/Picoclick-C3)
[https://github.com/joshr120/PD-Stepper/tree/main](https://github.com/joshr120/PD-Stepper/tree/main)
[https://github.com/uLipe/espFoC](https://github.com/uLipe/espFoC)
[https://github.com/Skythinker616/foc-wheel-legged-robot](https://github.com/Skythinker616/foc-wheel-legged-robot)
[https://github.com/matt-ressel/esp32-c3-zero-standby-sensor](https://github.com/matt-ressel/esp32-c3-zero-standby-sensor)
[https://github.com/espressif/esp-idf/tree/master/examples/bluetooth/nimble/power_save](https://github.com/espressif/esp-idf/tree/master/examples/bluetooth/nimble/power_save)
[https://github.com/espressif/esp-now/tree/master/examples/coin_cell_demo/bulb](https://github.com/espressif/esp-now/tree/master/examples/coin_cell_demo/bulb)
[https://github.com/espressif/esp-now/blob/master/examples/coin_cell_demo/switch/docs/button_in_matter_bridging.md](https://github.com/espressif/esp-now/blob/master/examples/coin_cell_demo/switch/docs/button_in_matter_bridging.md)
[https://oshwhub.com/esp-college/32bfc63ed181441a9a44da6cd2419809](https://oshwhub.com/esp-college/32bfc63ed181441a9a44da6cd2419809)
[https://oshwhub.com/esp-college/esp-ths](https://oshwhub.com/esp-college/esp-ths)
[https://www.eetree.cn/project/4598](https://www.eetree.cn/project/4598)
```
> 参考价值（eetree #4598，ESP32-S3+2×DRV8874+AS5047P，SimpleFOC方案）：
> 1. strapping 脚不要用外置电阻之外的弱上下拉，且**不要用作输入功能**（会被其他设备上电默认电平影响启动）；
> 2. DRV8874 的 PMODE **不要用电阻配置死**，直接接单片机控制；
> 3. 其电流采样直接使用 DRV8874 的 IPROPI 两路做低边采样，与本文档方案一致。
